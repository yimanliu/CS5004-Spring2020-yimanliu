public class BSTbyScoreNode extends Node{
    int leftCount;              // number of nodes in its' left subtree

    public BSTbyScoreNode(Player player, Node left, Node right) {
        super(player, left, right);
    }
}